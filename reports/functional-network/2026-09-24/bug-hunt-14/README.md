# Evidence for pass 14

All API responses, identities and failures in these logs are synthetic test fixtures.
Raw logs are copied byte-for-byte, including terminal carriage returns and whitespace.
Source fingerprints identify selected final tested files, not a clean release snapshot.
The base already contained uncommitted pass 13; its report remains immutable.
