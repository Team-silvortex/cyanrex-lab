# 项目进度状态

快照日期：**2026-09-24**
当前版本线：**0.4.2**

本文是 Cyanrex Lab 的能力级进度基线，用来说明目前可用的内容、主动保留的限制，以及下一阶段需要
决策的方向。详细信任边界与数据流仍以[系统架构](architecture.md)为准。

## 能力矩阵

| 领域 | 状态 | 当前范围 |
|---|---|---|
| 身份与授权 | 可用 | Argon2 密码、TOTP、Cookie Session、CSRF 来源检查、教师教学/部署权威与学生守卫；兼容旧管理员 |
| eBPF 工作台 | 可用 | Monaco 编辑、本地 Clang 诊断与补全、bpftool 执行、Aya tracepoint 执行、挂载管理、源码探针和内核事件流 |
| 教学流程 | 可用 | 五个自动验收实验、尝试记录、学生进度、教师总览、有上限的源码审阅、学生可见的教师评语，以及确认后从历史提交继续修改 |
| 事件与持久化 | 可用 | 用户隔离的事件中心与实时队列、共享惰性事件 JSON、FIFO 保留、有界筛选读取、WebSocket lag 显式关闭与最近历史恢复、PostgreSQL，以及文档化的内存/文件降级 |
| 本地 Runner | 可用 | 可替换驱动边界、全局/单用户租约、超时处理，并明确报告 `shared_kernel` |
| Runner Agent | 远程检查可用 | 签名注册、心跳、租约、取消、探针和隔离的只编译诊断；尚未开放远程 eBPF 加载 |
| 部署与分发 | 可用 | Docker、WSL2、原生 Linux、加固的可选编译 Agent，以及离线打包/安装工具 |
| 发布可追溯性 | `0.4.2` 源码元数据已同步；制品验收仍需单独完成 | Changelog/版本同步、注解 Tag 预检、源码/归档校验和元数据、逐镜像 Docker content ID、精确镜像安装，以及 Rust 原生证据/候选验证与安全、非覆盖解包；`0.3.0` 仅为 API 基线，发布源码 Tag 不等于通过制品验收 |
| 模块目录 | 可用、仅状态控制 | 启动时发现并校验版本化 v1 清单；生命周期保存在内存中，绝不执行目录代码 |
| JavaScript SDK | 内部包可用 | 类型化 ESM 客户端，提供 63 个生成的非 Agent operationId 调用、77 项 additive-only 命名空间基线与弃用政策、显式 `/openapi` 和 `/operations` 导出，并支持浏览器/Node Session、取消、下载、类型化错误与包消费冒烟 |
| API 契约 | 内部契约可用 | 生成 OpenAPI 3.1 并通过 `/openapi.json` 提供；路由/权限层/SDK/模型漂移及相对冻结 `0.3.0` 基线的破坏性变化会使质量门禁失败 |
| Terminal 页面 | 教师可用 | 提供权限明确的模块列出/启动/停止、结构化结果与历史，并安全跳转到 eBPF 实验工作区；它不是系统 Shell |

## 设置与 Runner 浏览器安全（收录于 0.4.2）

- [第 13 轮](functional-network-bug-hunt-13.md)将设置读取和顺序保存绑定到导航，严格校验回执，
  部分成功或结果不确定后必须显式刷新核对。
- [第 14 轮](functional-network-bug-hunt-14.md)先校验指标再渲染，限制刷新等待，标明旧样本，
  并将反馈与设置表单、Runner 面板隔离。
- [第 15 轮](functional-network-bug-hunt-15.md)校验 Runner 清单，要求探针/取消确认，拒绝过期
  目标和迟到结果；后台轮询不能解锁结果不确定的操作。
- 历史报告及冻结地图保留原始版本和指纹。这些是浏览器生命周期修复，不代表新增 Agent 权限、
  远程内核加载、分布式事务或制品验收；创建源码版本/标签不会更改在线部署。

## 事件存储确认（收录于 0.4.1）

- [第 09 轮](functional-network-bug-hunt-09.md)修复冷启动 DropNew 容量计数和持久写任务退出；
  [第 10 轮](functional-network-bug-hunt-10.md)让 HTTP 历史/导出/未读在存储故障或非法筛选时
  明确失败，不再返回空/部分成功。
- [第 11 轮](functional-network-bug-hunt-11.md)与[第 12 轮](functional-network-bug-hunt-12.md)
  要求 HTTP 已读、删除、保留设置确认存储后才成功。SQL 交接后即使取消仍保持账号顺序；调用方
  有界等待返回未确认，不冒充回滚。旧运行时降级与显式纯内存模式保留，修复重启不会自动对账。
- 升版前最终验证通过默认 Rust 310 项（80 项忽略）、显式事件 PostgreSQL 52 项、公共检查 69 项、
  前端 93 项、SDK 运行时 13/包检查 3 项及类型检查。历史报告与指纹保留原始 0.4.0 输入，
  不改写为 0.4.1 制品验收。
- 本补丁不改变在线部署。设置页导航/分步保存、跨 Engine 协调、持久重放及当前版本局域网/
  内核/分发验收仍需单独推进。

## 事件链路与构建占用（收录于 0.4.0）

- [第 07 轮](functional-network-bug-hunt-07.md)修复事件筛选、导出/删除准入、导航取消、
  已读确认与未读轮询，并提供四语言的明确错误提示。
- [第 08 轮](functional-network-bug-hunt-08.md)修复 SQL/JSON 落盘，通过有界队列、用户级准入
  和事务约束发布、删除、替换及保留策略顺序；真实 PostgreSQL 故障、顺序和取消回归已显式接入 CI。
- 开发/测试构建保留文件行号回溯，减少调试信息并关闭增量缓存；生成的 TypeScript 构建元数据
  不再纳入版本管理。正式发布构建配置不变。
- 原地图和抓虫证据保留记录时的 0.3.8/0.3.9 基线，不改写为 0.4.0 制品验收。教师权威、
  API 兼容性及现有运行部署不变；事件持久重放、跨进程协调及当前版本局域网/内核/分发验收仍需单独完成。

## 功能链路抓虫修复（收录于 0.3.9）

- [功能链路地图](functional-network.md)枚举 12 个模块、40 条流程和 69 条连接。六轮关联报告覆盖
  学习读取与私有落盘、Runner Agent 容量/租约/响应上限、编译诊断、编辑器与头文件归属、
  运行/卸载竞态，以及断点与事件恢复。
- 原始地图、源码指纹及六轮报告保留 0.3.8 升版前输入；相对冻结清单的版本及修复源码漂移是
  预期结果，不将旧快照改写为 0.3.9。
- 教师权威、用户归属隔离、显式操作确认和 API 兼容性不变。本地回归不代表当前版本的局域网/TLS、
  SSH 部署、真实内核或离线制品验收；本次源码发布不会修改运行中的部署。

## 认证与持久化修复（收录于 0.3.8）

- [模块与边界网络](testing-network.md)发现的 9 个问题全部修复：认证 4、脚本本地一致性 3、
  事件危险筛选 2。登录会话与账号删除以事务确认结果，脚本原子落盘后发布缓存，事件只删除
  匹配项、拒绝危险筛选，并保留仅存在于 SQL 的记录。
- 覆盖注册/TOTP 编码、已观察到的会话撤销、归一化账号变更，以及四语言退出失败/重试提示；
  认证、事件、脚本的真实 SQL 边界已显式接入 CI。
- 2026-09-09 升版前验证通过 290 个不同 Rust 用例、35 项模拟后端的浏览器用例、44 项前端单元、
  55 项工具检查及 16 项 SDK/包测试；2 项性能专用测试未执行。[原始报告](acceptance.md)保留
  0.3.7 输入与哈希，不改写为 0.3.8 制品验收。
- 真实局域网/TLS 接入、当前源码内核验收、实际 SSH 部署及事件异步队列/重启/故障切换验证仍待
  单独完成。本 patch 不变更现有运行部署。

## SSH 与课堂接入（2026-09-09，收录于 0.3.7）

- Rust SSH plan/apply 管理预先安装的离线包，严格校验主机密钥、绑定目标确认、版本与部署文件，
  不自动重试，不附加卷删除。
- 可选 `/join` 与最小发现描述，通过绑定用户名、10 分钟有效、单次消费的邀请接入学生。
  保留密码/TOTP、教师地址核实、CSRF、独立协议/能力兼容检查、撤销和 no-store。
- 当前是链接发现，不是 mDNS/扫网；安装包上传、裸机初始化、证书/入口配置、课堂成员移除与
  VM 隔离仍待实现。OpenAPI/SDK 现覆盖 68 个 Engine 操作、63 个非 Agent 操作。
- 前端构建支持显式 Engine 地址，旧镜像用于局域网前需重建；配置与验收边界见
  [课堂接入指南](classroom-connection.md)。
- 验证：完整安全门禁与最终后端回归通过，Rust 233 项（默认忽略 5 项外部集成/基准）、前端
  41 项、公共检查 51 项、SDK 13 项与类型检查/3 项包检查、18 个生产路由；27 项可选浏览器
  回归全部通过。首次 RustSec 拉取失败不计通过，后续 RustSec/npm 未报告漏洞。Compose 使用
  合成配置，SSH/浏览器使用假目标；未变更真实远程主机、部署、网络暴露、凭据或 VM。

## 教师权威（2026-09-09，收录于 0.3.7 的前一轮增量）

- 单人默认部署账号和旧管理员名单现在返回 `teacher`；教师无需另登管理员，即可管理模块/头文件、
  设置、Runner Agent 和终端；学生仍不能进入管理功能。升级前必须审查教师名单，所有列入账号
  都将获得完整部署权。既有凭据和历史记录不重写。
- 公开注册不能抢占教师/旧管理员预留名字或自选高权限角色；默认部署者不能删除自己。
  TOTP、CSRF、用户数据归属和重要操作确认保留。
- Docker、离线 Compose、native/WSL 都传递两份角色名单；四语言导航显示教师/学生身份，
  OpenAPI 保留旧分类标识，同时准确列出教师权限。
- 完整安全门禁通过：Rust 218 项（默认忽略 5 项外部集成/手工基准）、前端 38 项、公共工具
  49 项、SDK 检查和 17 个生产页面路由；24 项可选 Chromium 回归通过，覆盖教师管理与确认、
  学生菜单和四语言窄屏。浏览器 Engine 响应为合成数据，没有改动运行中的部署。
- RustSec 与生产 npm 依赖审计通过；首次 RustSec 下载因网络失败，未将失败计为有效结果。
  两份 Compose 配置仅用合成输入验证，没有启动容器。
- 本轮是角色/归属统一，不是 VM Runner、远程内核加载、课堂接入、浏览器角色编辑或部署重启；
  局域网仍以教师控制端为架构目标。见[教师指南](teacher-guide.md)和[课堂隔离](classroom-isolation.md)。

## 0.3.6 已验证基线

以下检查覆盖现已收录于 0.3.6 的操作确认、布局、历史提交恢复、冷加载增量及下方 0.3.5 的变更。
浏览器、真实集成、内核及 benchmark 证据采集于升版前，保留原始源码和二进制指纹，
不代表已经验收 0.3.6 发行制品。

- Rust 格式与锁定依赖的 Engine 测试：213 项通过；5 项测试在默认门禁中忽略，分别是外部
  PostgreSQL 和 Runner Agent 集成，以及三项需显式运行的手工基准。
- Next.js 生产构建：17 个静态页面路由，TypeScript 校验通过。
- 前端回归：37 项通过，覆盖权限、Terminal 命令、教师审阅/评语、历史提交恢复、安全操作范围、事件恢复、性能热点、安全响应头和 macOS
  元数据清理；SDK 另有 12 项传输/操作回归、1 组操作编译期约束与 3 项包清单/导入冒烟通过。
- 可选 Chromium 界面回归：更新后的生产构建通过全部 22 项；依赖更新前，12 项安全操作用例也
  通过了开发环境 Strict Mode 检查。四语言 320 px 弹窗边界自动检查通过，并完成最终中文截图复查。
- 文件长度、版本/Changelog/课程副本同步、OpenAPI 生成/路由/权限层/模型/兼容性检查，
  47 项公共工具回归，以及 Runner Agent 和分发工具检查。
- 生产 npm 依赖审计与 RustSec 审计通过，未报告漏洞。
  Next.js 15.5.24 和 sharp 0.35.4（内置 libheif 1.23.2）已替换此前存在告警的前端依赖锁定版本；
  运行中的部署仍需重建，见[安全指南](security.md)。

常规质量门禁没有启动特权 Engine，也没有运行会操作临时 Docker 栈的离线安装冒烟；另在获准的
一次性 VM 内执行了下方记录的源码级内核验收，不代表发行制品已验收。注解 Tag 候选流程会启用
发行包中的真实 Aya 挂载、ring-buffer 事件与精确 detach 检查，并保留绑定候选
产物的报告；其结果属于环境级证据，不计入上面列出的本地通过项。发行包内验证器无需内核权限，
即可复查 v1/v2 模式、发行元数据绑定、事件身份和清理结果；仓库侧验证器还能在不解包的情况下
复验整个下载制品，并能在不调用 `tar`、不覆盖已有输出的前提下只写出验证过的普通文件。

## 一次性 VM 内核验收（2026-09-09，收录于 0.3.6）

- 获准后用通过签名/校验和验证的 Ubuntu 镜像新建独立、受资源限制的 QEMU/KVM Guest。
  安装工具后先关机，再以受限用户网络启动；专项探针确认 Guest 不能访问宿主，回环 SSH 仍可用。
- 升版前的原生 dev 快照在 Ubuntu 24.04.5 / Linux 6.8.0-139 上通过真实 Aya 挂载 → 匹配 ring-buffer
  事件 → 准确 detach。没有残留 bpffs pin，前后内核程序/link ID 清单一致，Rust 证据验证器在
  Guest 和宿主两侧均通过。
- VM 和测试进程已停止，没有改动已有 VM 与部署。源码/二进制哈希及 `candidate: null` 的证据见
  [验收清单](acceptance.md)。这不代表新增 VM Runner、证明多学生隔离，或已验收离线包/Tag/局域网链路。

## 第一轮真实集成验收（2026-09-09，收录于 0.3.6）

- 显式运行默认门禁跳过的两项外部集成：一次性 PostgreSQL 16.14 测试 1 项通过，真实回环 HTTP、
  签名 Runner Agent 请求与 Clang 18 编译测试 1 项通过。
- PostgreSQL 在旧表迁移、并发版本冲突和持久化之外，补验历史提交读取归属、当前教师评语、
  不存在/他人记录，以及读取失败不降级到本地。这是服务层 SQL 验收，不是完整浏览器链路。
- Rust CI 新增独立、仅回环可达的 PostgreSQL 服务，先确认准确的忽略测试存在，再实际执行；
  公共回归检查此配置。配置已在本地检查，已记录的 PostgreSQL 结果属于本地验收，不是远端 CI 结果。
- 临时数据库已清理；现有容器、数据和内核挂载未改动。这不能证明 VM 隔离、局域网安全、发行包
  安装或真实内核验收完成。证据、复现方式及后续门槛见[验收清单](acceptance.md)。

## 重要操作确认与防误触（2026-09-09，收录于 0.3.6）

- 内核运行、卸载、脚本/头文件/事件删除、草稿替换、远程编译节点选择及高影响账号/管理操作，
  统一先审阅目标与影响，默认焦点在“取消”。批量清理需输入准确确认词；执行中阻止重复提交，
  失败后需核对状态并重新确认，不自动重试。只读查看仍可直接操作。
- 单项卸载必须有准确路径，不会退化为全部卸载。事件删除固定绝对截止时间，明确影响全部匹配
  记录而非最多 200 条可见数据。头文件批量删除遇首个失败即停止，显示已完成项，不声称回滚。
- 进入实验保留草稿，明确“加载实验模板”后才替换，运行仍需独立操作。导航会丢弃未确认操作和
  过期本地文件导入；自动诊断向远程 Agent 发送源码也需确认。已发出的 Engine 变更不因导航撤销。
- 新增 3 项纯函数回归检查准确卸载目标与删除范围；12 项可选 Chromium 检查
  （`npm --prefix frontend run test:safety-browser`）覆盖键盘焦点、取消、重复点击、失败、目标绑定、
  草稿/导航竞态、四语言及 320 px 弹窗，开发模式 Strict Mode 重放也保留确认和焦点。
  测试使用合成 Engine 响应，不执行真实账号、数据库、内核或局域网变更；后端授权与运行边界不变。

## 面向操作流程的界面布局（2026-09-08，收录于 0.3.6）

- 桌面导航滚动时保持可用；窄屏使用可展开菜单，支持 Escape 关闭、当前页语义和跳过导航链接，
  仍保留原有角色过滤与路由权限。
- 编辑器吸顶栏集中导入、保存和运行，诊断与结果紧跟源码；宽屏侧列提供运行设置和挂载清理，
  较小屏幕可用分区链接快速往返。脚本、元数据、头文件和断点详情按需展开，不重建 Monaco。
  同一 1440 × 900 合成用例中，源码起点从距页顶约 623 px 前移到 315 px；布局不改变运行逻辑、
  Session 或 API 契约。
- 学习进度移到资料之前，并提供历史记录/资料直达链接；教学概要紧凑排列，窄屏学生行转换为带字段名
  的卡片，选择学生后聚焦到复盘区。
- 五项可选 Chromium 布局检查（`npm --prefix frontend run test:layout-browser`）覆盖源码位置、吸顶操作、
  320–1440 px 宽度、四语言、菜单/分区导航、审阅焦点/草稿保留，以及导入/保存/加载不触发执行。
  原有恢复、评语和事件浏览器检查继续保留。浏览器 Engine 请求均为合成响应，不代表内核或局域网验收。

## 从历史提交继续实验（2026-09-08，收录于 0.3.6）

- “学习中心 → 我的实验记录 → 从这次提交继续”会在编辑器中预览源码和当前教师评语。
  明确确认后才恢复该次提交的代码和实验/模板上下文；保留草稿、加载失败、取消和过期响应不会
  替换代码，模板也不会覆盖恢复中的草稿。重新进入某条记录会再次要求确认。不自动执行、挂载
  或卸载；修改后手动运行会新增尝试，原记录继续保留。
- 新增按 Session 所有者查询的 `GET /learning/attempt?attempt_id=...`，返回 no-store 响应并
  显式报告存储错误；SDK 新增 `learning.attempt()` 和生成的 `getLearningAttempt`。教师查看他人
  源码仍使用单独授权的审阅接口，恢复接口不接受用户名覆盖。SQL/文件格式和已有 API 契约兼容。
- 新增 2 项 Rust 权限/错误回归、5 项前端请求/校验回归、1 项 SDK 传输回归及类型/包校验。
  完整安全质量门禁通过；4 项可选 Chromium 验收通过，覆盖确认、真实 Monaco 编辑、手动模拟运行、重试/取消、同页目标切换竞态
  和桌面/窄屏布局，并保留原有教师评语冒烟。浏览器使用合成 Engine 响应，不代表真实 PostgreSQL、
  内核或局域网执行验收。使用方式见[学生指南](student-guide.md)，教师文档和四语言界面已同步。

## 学习记录取消安全的首次加载（2026-09-08，收录于 0.3.6）

- 本地首读在取得锁后交给阻塞工作线程校验 UTF-8、解析 JSON，直接构造共享记录。初始化和提交
  共用同一把锁；排队/运行中的加载不会因请求取消而丢失结果，并发读者复用成功快照。只有
  `NotFound` 视为空 Store，其余 I/O/解析错误不发布部分结果，后续可重试。
- 新增 6 项 Rust 回归，覆盖取消、并发首读/写入、路径错误和旧解析行为差分，包含未知字段中的
  非法 UTF-8；连续运行 20 轮通过。新增 1 项 JS 回归验证五轮配对顺序，完整安全门禁通过。
- 最终 40 个进程的矩阵以 0.3.5 库为基线，使用相同的新只读程序，并在被测进程之外准备相同合成
  文件。5 万条、源码 4,184 字节时，2 ms 定时器最大延误的中位数从 218.641 降到 2.037 ms，
  峰值 RSS 从 461.7 降到 448.9 MiB，但首读从 405.9 增到 485.5 ms（+19.6%）；1 万条和
  较小源码的 5 万条首读也退化。这轮改善取消安全和执行器响应性，不代表初始化全面提速。
- 这是操作系统页缓存为热态的进程/Store 冷启动基准，不是冷磁盘、HTTP、PostgreSQL 或局域网
  验收。完整输入和解码记录仍同时驻留，没有新增大小上限；整文件写入、崩溃恢复、fsync 和跨
  进程协调边界未变。行为与复现见[学习记录存储](learning-storage.md)，证据在
  `reports/benchmarks/2026-09-08-learning-cold-load-final`；不兼容的字节解析原型另有 40 次进程
  结果保留，未覆盖源码指纹或测量数据；此增量现已收录于 0.3.6。

## 学习记录流式提交增量（2026-09-08，收录于 0.3.5）

- 本地写入改由阻塞工作线程，以 64 KiB 缓冲流式输出逐字节兼容的 pretty JSON；显式 flush 后
  rename，再发布内存。不再分配完整编码文件缓冲，但仍重写整文件，快照及 SQL/HTTP/SDK 契约不变。
- 交接前取得写锁，每个 Store 最多一个排队/运行的提交；请求取消后工作线程仍持锁完成发布，
  修复磁盘/内存分歧与后续旧快照覆盖。交接前取消不启动写入，交接后可能保存成功但没有响应，
  重试前应先刷新；未新增追加幂等或崩溃/断电保证。
- 新增 9 项 Rust 回归，覆盖等待写锁/线程池排队、追加与评语 rename 后取消、脱离请求后的失败、
  流式输出、字节兼容、中断/部分写入及 flush 错误，完整安全门禁通过。
- 新的 72 轮磁盘配对矩阵以上一轮共享快照版本为基线，而非干净发行版。5 万条、源码 1,112 字节
  时，写入平均耗时 144.6 → 111.3 ms，p95 152.5 → 122.1 ms；1 万条的写入进程峰值 RSS 从
  63.2 降到 39.2 MiB。小规模写入平均值/p50 及部分未改动读取对照退化，大源码磁盘尾延迟波动明显，
  不能将本机结果解读为全面提速。
- 源码指纹和原始记录在 `reports/benchmarks/2026-09-08-learning-streaming-writes`，另有独立的
  72 轮 tmpfs 对照；取消、重试与关闭限制见[学习记录存储](learning-storage.md)。fsync、恢复日志、
  增量持久化及跨进程协调仍待推进。历史基准保留测量时的源码/版本指纹，不随 patch 升版重写，
  也不作为发行制品验收证据。

## LearningStore 快照与查询增量（2026-09-08，收录于 0.3.5）

- 本地读取共享不可变快照；最近审阅先有界筛选引用，再复制响应记录；学生进度和教师总览单次
  遍历聚合，不再克隆提交源码。追加只复制指针索引；评语额外复制被修改记录，旧读者快照不变。
- 新增 12 项 Rust 回归和 2 项基准排序回归，完整安全门禁通过。普通 pretty JSON、旧记录、SQL
  查询、授权、评语版本冲突和响应模式保持兼容，覆盖写入/重命名失败、并发更新与重载。
- 修正配对顺序后的 72 轮磁盘对比中，5 万条、源码 1,112 字节时，最近 20 条 p95 从 1.744 ms
  降到 0.220 ms，教师总览从 41.907 ms 降到 4.446 ms；写入进程峰值 RSS 从 296.3 MiB 降到
  180.1 MiB。这是本机热态服务基准，不是 HTTP 延迟或课堂容量结论。
- 磁盘写入并非全面提速：5 万条、源码 4,184 字节时，写入平均耗时增加 28%。另做 72 轮 tmpfs
  对照虽有改善，但不能证明持久化磁盘性能；初版存在配对顺序缺陷的 72 轮数据单独保留，未合并或覆盖。
- 该测量阶段仍使用完整编码文件缓冲，rename/内存发布仍可被取消；上方后续增量替换了这两处机制，
  原始历史测量不重写。仍需整文件替换、常驻记录、指针索引复制和旧读者快照，未新增 fsync、恢复
  日志或跨进程协调。
- 行为和限制见[学习记录存储](learning-storage.md)，报告在
  `reports/benchmarks/2026-09-08-learning-snapshots-final`。

## 用户级事件分发增量（2026-09-08，收录于 0.3.5）

- `/ws/events` 按已认证会话用户建立有界实时队列，他人的突发流量不能挤掉本人排队事件；
  本人过载关闭、认证/来源检查和原始 JSON 保持兼容，旧 EventBus 全局服务订阅接口仍可用。
- 同一用户的连接共享不可变事件及惰性 JSON 编码；最后一个连接关闭/取消时清理队列，并处理
  并发重连时旧连接误删新队列的竞态。
- 新增 10 项回归，完整安全门禁通过。最终完成 30 轮 release 实验：24 轮当前分发路径对比、
  6 轮无订阅者二进制前后对照。在匹配输出一致时，单用户服务/数据帧构造吞吐提升约 24%–64%，
  8 用户提升约 40%–56%；另有 9 轮真实回环检查确认交付、显式过载和约 5 秒的慢连接清理。
  未缓存 JSON 的初版曾出现单用户退化，其结果保留而未被覆盖。
- 以上是本机合成基准，不代表局域网/内核或发行验收。容量按活跃用户计算，队列和 JSON 缓存
  内存随活跃用户及载荷增加；锁、持久化和 CPU 仍共享，尚未增加全局准入或持久化重放。
- 行为和复现命令见[事件流恢复](event-stream.md)，报告在
  `reports/benchmarks/2026-09-08-owner-fanout-final` 与 `2026-09-08-owner-event-stream`。

## 事件性能与恢复增量（2026-09-08，收录于 0.3.4）

- 使用 FIFO 双端队列避免满历史头部搬移，有限快照先筛选引用再复制结果，并增加隔离的 release
  基准。历史基准保留采集时的源码/版本指纹，不随 patch 升版重写，也不作为发行制品验收证据。
- WebSocket lag 改为显式关闭，阻塞发送超时回收；两个浏览器消费者支持有界快照、旧请求作废、
  自动重连及持续可见的缺口提示，恢复范围仅限仍保留的最近历史。
- 携带会话的 WebSocket 握手也检查 Origin/Referer，原生客户端需提供允许的来源。
  兼容要求和限制见[事件流恢复](event-stream.md)。
- 另行通过九轮真实回环压力测试，以及使用模拟 HTTP/WebSocket 的 Chromium 生产页面冒烟。
  这些 0.3.4 测量仍使用全局广播，用户级分发见上方 0.3.5 增量；持久化游标重放仍待推进。
- 修复 Docker 构建输入遗漏及 npm 审计瞬态失败的有限重试处理。

## 教学主线增量（2026-09-05，收录于 0.3.3）

- 教师/管理员可以给已有学生提交保存当前评语，学生从“学习中心 → 我的实验记录”读取；
  支持多行纯文本、作者/修改时间和 2000 字符上限，自动验收结果和原始源码保持不变。
- 写入采用版本号检查，过期修改返回 `409`，页面保留草稿并支持加载最新评语后重新确认；
  本地文件写入失败不发布未保存状态，PostgreSQL 写入失败不转写本地副本。
- 新增 8 项后端路由回归、2 项前端请求构造回归和 2 项 SDK 回归；另行通过一次性 UTF-8
  PostgreSQL 的旧表迁移/并发更新/失败不降级测试，以及模拟 Engine API 的 Chromium 页面交互测试。
- 本轮仅扩展教学反馈闭环，未改变特权内核执行或远程 Agent 的信任边界。

## 局域网/桌面 Runner 准备（2026-09-06，收录于 0.3.3）

- 已记录 [Linux 桌面/局域网隔离目标](classroom-isolation.md)：教学控制与学生独占 VM 分离，
  设计允许桌面机承载虚拟化环境。
- Runner 执行校验租约所有者；挂载清单、卸载和清理报告走选定驱动，带操作超时，后端失败不退回本机。
- 新增归属、生命周期、超时、认证/CSRF 和本地适配器回归覆盖。
- VM 创建/加载、持久化环境归属、完整运行时/事件代理和局域网入口加固仍待完成；本轮没有把本地
  Engine 变成学生安全沙箱。

## 编译 Runner 增量（2026-09-06，收录于 0.3.3）

- 检查/补全路由将 Session 用户、源码、所选头文件和光标位置交给选定驱动，保留诊断结果及可选
  缓存状态，不退回本地编译器。
- 编译容量归属各 Manager（2 个检查、3 个补全），操作期限和取消安全的统计/名额管理防止请求
  卡住后长期占用容量或虚增进行中计数。
- 本地适配器按用户隔离缓存键，正常返回或取消时清理私有源码工作区；新增 4 项路由、2 项缓存归属、
  2 项工作区和 1 项必需用户归属回归。
- 挂载验证、事件流、环境/设置、远程头文件交付和 VM 生命周期仍待实现；实际运行端仍在本机共享内核。

## 主动保留的边界

- 特权 Engine 面向可信自部署教学环境，不面向公网多租户。
- 本地执行共享同一个 Linux 内核；配额是资源控制，不等于学生隔离。
- Engine 仍是单进程。Agent 注册、远程作业、挂载和模块生命周期尚无多副本协调模型。
- Aya 当前覆盖已支持的 tracepoint 路径，bpftool 仍是兼容性最广的路径。
- 远程 Agent 只编译和诊断，`/ebpf/run` 仍在本地执行。

## 下一阶段决策点

1. 在把仅状态控制的模块目录扩展为进程或动态库插件前，先定义签名、真实隔离、可执行适配器与
   持久化所有权模型。
2. 在将 SDK 作为稳定、独立消费的软件包发布前，明确 Registry 发布、支持责任与 1.0 就绪标准。
3. 按桌面/局域网隔离目标补齐完整运行边界、持久化环境归属和 VM 恢复，再开启远程 eBPF 执行或
   Engine 多副本。
4. 在明确发布信任链与密钥归属后，对已验收候选产物签名并自动发布。
5. 收集首个注解 Tag 的真实内核验收证据，再决定是否需要在 GitHub 托管的特权 Docker 环境之外
   增加专用、自托管的多内核版本矩阵。

`engine/Cargo.toml` 是正式版本的唯一基准；`scripts/check-version-sync.sh` 会阻止前端、SDK、
OpenAPI 文档、锁文件和面向发布的文档再次漂移。
